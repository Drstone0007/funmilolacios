import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fumilola.home',
  appName: 'FUMILOLA HOME FOR AGED',
  webDir: 'dist'
};

export default config;
